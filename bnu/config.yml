default:
  # put all the required where filters here
  where:
    # examples (you should make sure these filters are valid):
    # - table: course
    #   field: Name
    #   operator: REGEXP
    #   values: .*
    # - table: content
    #   field: Name
    # # operator of `=` or `IN` can be neglected
    #   values:
    #   - 萤火虫
    #   - 超级秒表
    - table: base_organization
      field: Name
      values: 北京师范大学认知实验
    - table: v_organizationuser
      field: GradeName
      operator: REGEXP
      values: 2.+级
  require_keyboard:
    - 快速归类PRO
    - 变色魔块PRO
    - 多彩文字PRO
    - 卡片分类PRO
    - 候鸟迁徙PRO
    - 声调判断
    - 语义判断
    - 一心二用PRO
    - 察颜观色PRO
    - 太空飞船PRO
    - 专注大师_中级
    - 数字卡片
    - 格子卡片
    - 美术卡片
    - 文字卡片
